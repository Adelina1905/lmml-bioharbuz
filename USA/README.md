The code requires the mp3 file to be added to the folder before running. couldn't add it because it's too large
after having the audio file in, just run the main
it will split the auti dile in smaller chunks and go through all of them looking for key words.
if it finds it, the paragraph will be printed from which you can extarct the keyword letter.
